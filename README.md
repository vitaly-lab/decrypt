
* ## Exercise

~~~
Write a Spring boot application, a controller with two operations - encrypt file, decrypt file,
the controller accepts the file, encrypts it by byte shift by 5, and decrypts it using the same method.
The encrypted file is written to the database.
~~~

